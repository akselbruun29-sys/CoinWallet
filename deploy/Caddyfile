# Local / LAN reverse proxy with optional TLS (set SITE_ADDRESS in .env)
{$SITE_ADDRESS:localhost} {
	encode gzip

	handle /api/* {
		reverse_proxy api:8001
	}

	handle {
		reverse_proxy admin:5173
	}
}
